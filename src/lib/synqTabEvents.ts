/** Emitted when the Synq tab bar button is long-pressed. */
export const SYNQ_TAB_LONG_PRESS = "synqTabLongPress";

/**
 * Emitted when a friend activates, deactivates, or nudges on Synq via push.
 * Payload: `{ fromUserId?: string; inactive?: boolean }`.
 */
export const SYNQ_ACTIVE_FRIENDS_REFRESH = "synqActiveFriendsRefresh";
